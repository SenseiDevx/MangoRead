import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        green: {
            main: '#2FE09B', // Задайте желаемый цвет для кнопок
        },
        purple: {
            main: '#AD02E0'
        }
    },
});

export default theme;
